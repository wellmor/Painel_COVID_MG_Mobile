import styled from 'styled-components';

export const SplashContainer = styled.View`
  flex: 1;
  /* background: ${({ theme }) => theme.BACKGROUND_APP}; */
  justify-content: center;
  align-items: center;
`;
